function:
  name: ${self:service}-${self:provider.stage}-DashboardOverview
  description: Get dashboard overview with all statistics, triggered by API Gateway
  handler: src.entrypoints.apigw.routes.dashboard.handler
  events:
    - http:
        method: GET
        path: /dashboard/overview
        cors: true
  environment:
    POWERTOOLS_SERVICE_NAME: dashboard
    JWT_SECRET_KEY: ${env:JWT_SECRET_KEY, "dev-secret-key-change-in-production"}
    JWT_ALGORITHM: ${env:JWT_ALGORITHM, "HS256"}
